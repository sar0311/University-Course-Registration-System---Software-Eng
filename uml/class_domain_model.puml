@startuml
title Domain Class Model – University Course Registration System

' ===== Superclass =====
abstract class User {
  userId: String
  name: String
  email: String
  + login()
  + logout()
}

' ===== Subclasses =====
class Student {
  + registerCourse()
  + dropCourse()
  + viewSchedule()
}

class Advisor {
  + approveRegistration()
  + rejectRegistration()
}

class Administrator {
  + manageCourses()
  + manageSections()
}

User <|-- Student
User <|-- Advisor
User <|-- Administrator

' ===== Core Domain Entities =====
class Course {
  courseCode: String
  title: String
  credits: int
}

class Section {
  sectionId: String
  schedule: String
  capacity: int
  + checkAvailability()
}

class Enrollment {
  enrollmentId: String
  status: String
  + confirm()
  + cancel()
}

class Payment {
  paymentId: String
  amount: double
  status: String
  + processPayment()
  + refund()
}

' ===== Relationships =====
Course "1" o-- "1..*" Section : aggregation
Section "1" *-- "0..*" Enrollment : composition
Student "1" -- "0..*" Enrollment
Enrollment "0..1" -- "1" Payment

@enduml
